# pnpm configuration for TranslatorPad AI

# Auto install peer dependencies
auto-install-peers=true

# Strict peer dependencies (disabled for compatibility)
strict-peer-dependencies=false

# Hoisting for Electron compatibility
hoist=true
hoist-pattern[]=*electron*
hoist-pattern[]=*@electron*

# Registry
registry=https://registry.npmjs.org/ 